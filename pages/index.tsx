import {MainPage} from "./ui/main-page";
import {Template} from "../client/components";

export default function Home() {
    return (
        <Template>
            <MainPage/>
        </Template>
    );
}
